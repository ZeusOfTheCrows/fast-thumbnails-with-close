Fork to add the ability to close the currently selected window with `Del` or `Q`. Not currently configurable as this should probably be in kwin rather than the task switcher itself.

fix to workaround this issue i was having in kde:
https://bugs.kde.org/show_bug.cgi?id=479250
